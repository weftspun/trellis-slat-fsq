# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/

# Environments / caches
.venv/
.pixi/
.pytest_cache/
.ruff_cache/

# Lean / Lake (witness proofs)
.lake/
lake-packages/
build/

# Data / artifacts (keep the repo code-only)
*.parquet
*.pt
*.ckpt
lake/

# Elixir
/_build/
/deps/
/cover/
erl_crash.dump
*.ez
mix.lock

# NIF build artifacts (rebuild via native/build_windows.ps1)
priv/*.dll
priv/*.lib
priv/*.exp
native/*.gen.cpp
native/*.obj
erl_crash.dump
.slangtorch_cache/
.venv-slang/
